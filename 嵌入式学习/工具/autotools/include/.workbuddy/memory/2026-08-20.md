# 2026-08-20

## autotools 项目修复（工具/autotools）

- kohju 在 WSL 跑 `./configure` 报 No such file or directory，装包又把 `automake` 拼成 `autmake`。
- 根因：项目缺 `configure.ac`（configure 是 autoreconf 生成物，不是装出来的）；且根目录的 Makefile.am 内容其实是 src 的，放错了位置。
- 已补齐：`configure.ac`（AM_INIT_AUTOMAKE([foreign])，跳过 NEWS/AUTHORS 等检查）、顶层 `Makefile.am`（SUBDIRS = lib src）、`src/Makefile.am`、修正 `lib/Makefile.am` 的 include 路径，并填充了空源文件（head.h/haircut.c/havemeal.c/main.c，liblife 静态库示例）。
- 注意：目录名 `includ` 少了个 e（教程应为 include），Makefile.am 中路径已按 `../includ` 写；若改名需同步改两处 -I 路径。
- 待用户在 WSL 自行验证：`sudo apt install autoconf automake libtool` → `autoreconf -i` → `./configure` → `make` → `./src/main`。（本机 WSL 调用被安全策略禁用，无法代跑）

## 目录更正：includ → include（10:22）

- 应 kohju 要求把 `includ` 更正为 `include`：head.h 与 .workbuddy 已迁入新 `include/`，两处 Makefile.am 的 -I 路径同步改为 `../include`。
- 旧的空 `includ` 目录被 IDE 锁定（是当前打开的工作区文件夹）删不掉，已空、无影响；用户关闭/重开工作区后可手动删除。
- 注意：工作区 memory 实际位置已变为 `autotools/include/.workbuddy/memory/`。

## 生成 autotools 帮助文档（10:31）

- 应 kohju 要求生成 `autotools/autotools_help.html`：cheat-sheet 风格 HTML（工具链全景 SVG、七工具职责表、开发/用户两条流程、configure.ac 宏表、Makefile.am 变量表、make 目标、liblife 实战样例、报错对照表、交叉编译三件套）。以后 autotools 相关参考直接指向该文件。
